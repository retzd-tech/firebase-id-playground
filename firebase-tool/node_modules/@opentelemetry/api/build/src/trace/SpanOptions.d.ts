import { TimeInput } from '@opentelemetry/api/build/src/common/Time';
import { SpanAttributes } from '@opentelemetry/api/build/src/trace/attributes';
import { Link } from '@opentelemetry/api/build/src/trace/link';
import { SpanKind } from '@opentelemetry/api/build/src/trace/span_kind';
/**
 * Options needed for span creation
 */
export interface SpanOptions {
    /**
     * The SpanKind of a span
     * @default {@link SpanKind.INTERNAL}
     */
    kind?: SpanKind;
    /** A span's attributes */
    attributes?: SpanAttributes;
    /** {@link Link}s span to other spans */
    links?: Link[];
    /** A manually specified start time for the created `Span` object. */
    startTime?: TimeInput;
    /** The new span should be a root span. (Ignore parent from context). */
    root?: boolean;
}
//# sourceMappingURL=SpanOptions.d.ts.map