import { Subject } from '@angular-devkit/core/node_modules/rxjs/internal/Subject';
import { Subscriber } from '@angular-devkit/core/node_modules/rxjs/internal/Subscriber';
import { Subscription } from '@angular-devkit/core/node_modules/rxjs/internal/Subscription';
/**
 * A variant of Subject that requires an initial value and emits its current
 * value whenever it is subscribed to.
 *
 * @class BehaviorSubject<T>
 */
export declare class BehaviorSubject<T> extends Subject<T> {
    private _value;
    constructor(_value: T);
    readonly value: T;
    /** @deprecated This is an internal implementation detail, do not use. */
    _subscribe(subscriber: Subscriber<T>): Subscription;
    getValue(): T;
    next(value: T): void;
}
