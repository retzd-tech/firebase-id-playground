# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Run all unit test using Firebase Emulator on Push
'on':
  push:
    branches:
      - master
      - STORY-*
jobs:
  emulator_unit_testing_push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: "16"
      - name: Install Firebase Tools
        run: cd $(npm root -g) && pwd
      - name: Install Dependancies on root
        run: echo "npm install"
      - name : Run tests with imported data
        run: /opt/hostedtoolcache/node/16.19.1/x64/lib/node_modules/firebase-tools/lib/bin/firebase.js emulators:exec --project indonesia-firebase-playground --import="emulators-data" --only firestore 'npm run test'
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}